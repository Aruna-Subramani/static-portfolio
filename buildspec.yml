version: 0.2

phases:
  pre_build:
    commands:
      - echo "we are in pre-build phase"
  build :
    commands:
      - echo "we are in build phase"
  post_build:
    commands:
      - echo "we are in post-build stage"
artifacts:
  files: 
    - '**/*'
      
